#application
spring: 
  application:
    name: turbine
  profiles:
    active: local
  cloud:
    config:
      name: zwl-base, turbine
      discovery:
        enabled: true
        service-id: config-server

#server setting
server: 
  port: 8300
management: 
  port: 28300

#logging file
logging: 
  file: D:/zwl/logs/turbine/turbine.log